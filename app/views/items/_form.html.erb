<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

  <div class="btn-group">   <!-- fix: we defined categories all in items controller -->
    Categories
    <select class="span2">
      <% @categories.each do |category| %>
          <option value="<%= category.id %>"><%= category.title %></option>
      <% end %>
    </select>

  </div>



    <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :buy_price %><br />
    <%= f.text_field :buy_price %>
  </div>
  <div class="field">
    <%= f.label :minimum_bid_price %><br />
    <%= f.text_field :minimum_bid_price %>
  </div>
  <div class="field">
    <%= f.label :bid_duration %><br />
    <%= f.number_field :bid_duration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
